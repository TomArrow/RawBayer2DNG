<Window x:Class="RawBayer2DNG.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RawBayer2DNG"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" >
                    <WrapPanel>
                        <Label>Current image:</Label>
                        <TextBox x:Name="currentImagNumber" MinWidth="50" TextAlignment="Right">0</TextBox>
                        <TextBlock Padding="5 2 5 2"> of </TextBlock>
                        <TextBlock x:Name="totalImageCount" Padding="5 2 5 2">0</TextBlock>
                    </WrapPanel>
                    <Slider VerticalAlignment="Bottom"  Name="slide_currentFile" Value="{Binding ElementName=currentImagNumber,Path=Text,Mode=TwoWay}" Maximum="0" Minimum="0" TickPlacement="BottomRight" TickFrequency="1" IsSnapToTickEnabled="True" SmallChange="1" LargeChange="100" ValueChanged="Slide_currentFile_ValueChanged" ></Slider>
                </StackPanel>
                <Image Grid.Row="1" x:Name="mainPreview" MinHeight="300"></Image>
            </Grid>
        </StackPanel>
        <StackPanel Grid.Column="1">

            <WrapPanel>
                <Label>Raw resolution</Label>
                <TextBox x:Name="rawWidth" MinWidth="20">2448</TextBox>
                <Label> x </Label>
                <TextBox x:Name="rawHeight" MinWidth="20">2048</TextBox>
                <Label>(set/check resolution first)</Label>
            </WrapPanel>
            <Button x:Name="btnLoadRAW" Click="BtnLoadRAW_Click">Load single .RAW</Button>
            <Button x:Name="btnLoadRAWFolder" Click="BtnLoadRAWFolder_Click">Select folder of .RAW files</Button>
            <Button x:Name="btnLoadTargetFolder" IsEnabled="False">Select target folder</Button>
            
            <WrapPanel>
                <CheckBox x:Name="previewDebayer"></CheckBox>
                <Label ToolTip="does not affect resulting DNG">Preview debayer</Label>
            </WrapPanel>
            <WrapPanel>
                <CheckBox x:Name="previewGamma" IsChecked="True"></CheckBox>
                <Label ToolTip="does not affect resulting DNG">Preview with 2.2 gamma</Label>
            </WrapPanel>
        </StackPanel>
    </Grid>
</Window>
